# Claude Code 指示書：Colabデータセットパイプライン構築

## ⚠️ 作業開始前に必ず行うこと

```
最初に以下を実行してプロジェクト全体を把握してから、提案・実装に着手すること。
絶対に既存ファイルへの破壊的変更を行わないこと。
```

1. プロジェクトのディレクトリ構造を全て確認する（`find . -type f | head -100` 等）
2. 既存のデータセット・インデックスファイルの場所と形式を確認する
3. 既存の `.md` ファイル（設計書・README等）を全て読み込む
4. 現状のFAISSインデックス構成（Layer A / Layer B）の実装を確認する
5. 現状のデータパイプライン・スクリプトが存在する場合は内容を確認する

---

## ① プロジェクト理解フェーズ（必須）

以下を把握した上で次フェーズに進むこと：

- 現在のディレクトリ構成と各モジュールの役割
- 既存データセットの形式・スキーマ・保存場所
- `.md` 設計ドキュメントに記載された想定構造
- **設計ドキュメントの想定構造と実際の実装との乖離（Gap）**
- 不足しているデータソース・マッピング・中間テーブルの洗い出し

> ✅ チェックポイント：理解した内容を箇条書きで報告してから次に進む

---

## ② 全体方針

### データフローの方向性

```
[Google Colab]                          [FastAPI本体]
  元データ収集                              ↑
  → データ統合・クレンジング         完成済みデータセット
  → FAISSインデックス構築    ──────→  + FAISSインデックス
  → マッピングテーブル生成             を参照・検索
  → Google Drive に保存
  → GitHub push (スクリプト)
```

- **Colab の役割**：元データ収集・統合・前処理・インデックス構築・更新管理
- **FastAPI の役割**：完成済みデータセットとインデックスを参照して判定処理を実行
- **GitHub の役割**：VS Code ↔ Colab 間のスクリプト同期媒体
- **Google Drive の役割**：生成物（インデックスファイル・データセット）の受け渡し場所

---

## ③ 絶対に守ること（データ保護原則）

```
既存のデータセット・FAISSインデックス・DBは、
新しいパイプラインが完全に動作確認されるまで一切変更・上書きしない。
```

具体的には：

- 新規データは必ず別ディレクトリ（例: `data/staging/`）に出力する
- 本番データへの反映は明示的な手動スイッチ（フラグ or コマンド）で行う設計にする
- 既存の `*.index`、`*.db`、`*.csv` ファイルへの自動上書きは禁止
- 新旧データセットの並行稼働ができる構成にする

---

## ④ 診断・設計フェーズ

プロジェクト理解後、以下の診断レポートを作成すること：

### 診断レポートに含めるべき内容

```markdown
## 現状診断レポート

### 1. 現在のデータセット構造
- 各データソースの現状（ファイル形式・件数・更新日等）

### 2. 設計ドキュメントとのGap
- 設計書に記載があるが未実装のデータ・マッピング
- 実装されているが設計書に記載のないもの

### 3. 不足データ・マッピング
- 取得が必要な元データソース一覧
- 作成が必要な中間マッピングテーブル

### 4. 推奨アプローチ
- 優先順位付きの実装ロードマップ
```

> ✅ チェックポイント：診断レポートを提示・合意した後に実装に進む

---

## ⑤ データソースと対象インデックス

### 必須データソース

| データソース | 用途 | 優先度 |
|------------|------|--------|
| 貨物等省令（外為法） | 日本の該非判定マトリクス | 最高 |
| 輸出令（外為法） | 規制品目リスト | 最高 |
| BIS CCL（EAR / ECCN） | 米国輸出規制分類 | 最高 |
| HSコード（関税率表） | 品目分類・バージョン管理 | 高 |
| 特許データ（JP・US） | FAISSエビデンスチャンク | 高 |
| UN Comtrade API | HSコードの国別マッピング | 中 |

### FAISSインデックス拡張検討

現在の Layer A / Layer B 構成に加え、以下のインデックス追加を検討・評価すること：

#### 検討対象インデックス

| インデックス候補 | 内容 | 判定精度への貢献 |
|--------------|------|--------------|
| **FI（File Index）** | 特許のファイルインデックス分類 | IPC補完・技術分野の精度向上 |
| **Fターム** | 日本特許の多軸技術分類 | 技術的観点からの絞り込み精度向上 |
| **ECLA / CPC** | 欧州・国際特許分類 | JP/US以外の技術カバレッジ拡大 |
| **技術分野クロスマッピング** | ECCN ↔ IPC ↔ FI ↔ Fタームの対応表 | 説明根拠の多角的補強 |
| **規制キーワード辞書** | 外為法・EAR固有の技術用語 | 検索クエリの精度向上 |

#### 評価基準

各インデックス追加にあたり以下を評価して提案すること：
1. FAISSのコサイン類似度スコアへの寄与度
2. 判定根拠の説明可能性（Explainability）への貢献
3. データ取得・更新の現実的なコスト
4. 既存 Layer A / Layer B との統合方法

> 評価結果をもとに、追加するインデックスの優先順位と統合設計を提案すること

---

## ⑥ 実装スコープ（Colabパイプライン）

### 作成すべきファイル構成

```
scripts/
├── colab/
│   ├── 00_setup.ipynb              # 環境構築・認証
│   ├── 01_collect_raw_data.ipynb   # 元データ収集（外為法・EAR等）
│   ├── 02_integrate_datasets.ipynb # データ統合・クレンジング
│   ├── 03_build_mappings.ipynb     # マッピングテーブル生成
│   ├── 04_build_faiss_index.ipynb  # FAISSインデックス構築
│   └── 05_export_to_fastapi.ipynb  # 成果物エクスポート・Drive保存
├── pipeline/
│   ├── collect/                    # 各データソース収集スクリプト
│   ├── transform/                  # 変換・クレンジング処理
│   ├── index/                      # FAISSインデックス管理
│   └── export/                     # FastAPI向けエクスポート
data/
├── staging/                        # Colab生成物の受け取り場所（新規）
├── artifacts/                      # 本番反映済みデータ（既存維持）
└── raw/                            # 元データキャッシュ
```

### 各notebookの設計方針

- **冪等性**：何度実行しても同じ結果になること
- **再開可能**：途中で失敗しても途中から再実行できること
- **dry-run オプション**：`DRY_RUN=True` で本番データに触れずテスト実行可能
- **ログ出力**：処理件数・エラー・スキップ件数を必ず記録

---

## ⑦ GitHub同期フロー

```bash
# VS Code側（Claude Codeで開発・push）
git add scripts/ && git commit -m "update pipeline: add FI index builder" && git push

# Colab側（先頭セルで常に最新取得）
!git -C /content/[repo-name] pull || git clone https://github.com/[repo] /content/[repo-name]
```

---

## ⑧ 作業進行ルール

1. **提案 → 確認 → 実装** の順で進める（いきなり実装しない）
2. 既存ファイルを変更する前に必ず変更内容を提示して承認を得る
3. 各フェーズ完了時にチェックポイントを報告する
4. 不明点・判断が難しい設計上の選択肢は実装前に必ず質問する
5. 診断レポートは実装開始前に必ず提出する

---

## 補足：FastAPI側の対応（最小限）

Colabパイプラインが完成するまでは FastAPI 側の変更は最小限に留めること。
パイプライン完成後に以下を実装する：

```python
# utils/artifacts.py （新規作成のみ・既存コード変更なし）
# data/staging/ からデータを読み込む薄いユーティリティ層
# 本番切り替えは環境変数 USE_STAGING_DATA=true/false で制御
```
