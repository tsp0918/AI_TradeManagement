# UI Design Handoff — AI Trade Management Platform
## for Claude Design review session

---

## 1. プロダクト概要

**AI Trade Management** は、日本の輸出管理規制（外為法・EAR/ECCN・Wassenaar等）への対応を自動化する B2B コンプライアンス SaaS プラットフォームです。

- **主な利用者**: 日本のメーカー・商社の輸出管理担当者（法務・輸出部門）、R&D チームリーダー
- **コア業務フロー**: R&D 起案 → 品目登録・HS 分類 → AI 該非判定 → 制裁リストスクリーニング → 取引審査 → 輸出許可申請
- **アクセス**: Cloudflare Tunnel 経由で外部公開（`https://app.tsp-aitrademanagement.com`）
- **技術スタック**: FastAPI + Jinja2 テンプレート（SSR）、inline CSS、FAISS ベクター検索

---

## 2. 現在のアーキテクチャ

### シェル構造（ポータル）

```
┌──────────────────────────────────────────────────────┐
│  Header: "AI Trade Management" + バージョン + ?チャット │
├─────────────┬────────────────────────────────────────┤
│  Sidebar    │  <iframe> — 各モジュールの UI           │
│  (236px)    │  ポート 8000/8002/8003... のコンテンツ  │
│             │  ※ iframe の中にもモジュール固有の      │
│  業務フロー │    ヘッダー/ナビゲーションが存在する   │
│  受注後ERP  │                                        │
│  専門ツール │                                        │
│  管理       │                                        │
└─────────────┴────────────────────────────────────────┘
  DAP チャットウィジェット（全モジュールに浮遊）
```

### モジュール一覧（独立した FastAPI サービス）

| セクション | モジュール | ポート | 主要画面 |
|-----------|-----------|--------|---------|
| 業務フロー | R&Dリスク評価 | 8003 | プロジェクト一覧・バージョン詳細・みなし輸出 |
| 業務フロー | 品目管理 | 8002 | 品目一覧・編集・HS 分類・国別規制プロファイル |
| 業務フロー | AI該非判定 | 8011 | 取引一覧・新規審査・2リスト照合結果 |
| 業務フロー | 役務取引管理 | 8011 | 役務申請・みなし輸出台帳 |
| 業務フロー | 取引先スクリーニング | 8005 | 一括照合・結果一覧 |
| 業務フロー | 取引審査キュー | 8000 | ERP 伝票の承認キュー |
| 受注後ERP | 品目バージョン管理 | 8000 | 仕様変更履歴・影響アセスメント |
| 受注後ERP | 輸出許可申請 | 8000 | ドラフト生成・価値消費・期限管理 |
| 受注後ERP | 調達・BOM管理 | 8000 | BOM ツリー・De Minimis 計算・サプライヤー申告 |
| 受注後ERP | コンプライアンス進捗 | 8000 | 7ステージパイプライン・アクション管理 |
| 受注後ERP | EPA/FTA 特恵税率 | 8000 | HS×仕向地 特恵税率照会 |
| 専門ツール | 特許検索 | 8004 | BigQuery/J-PlatPat 検索・制裁照合 |
| 専門ツール | HSコード判定 | 8006 | FAISS ベクター照合 |
| 専門ツール | 規制レジーム照合 | 8000 | ITAR/EAR/EU/MTCR 一括照合 |
| 専門ツール | 与信管理 | 8000 | 取引先財務・制裁統合スコア |
| 専門ツール | 経済安保法チェック | 8003 | 特許非公開リスク・ICP 自己診断 |
| 管理 | 案件ダッシュボード | 8000 | 進行中案件・規制動向アラート |
| 管理 | プラットフォーム管理 | 8000 | モジュール状態・モジュールレジストリ |

---

## 3. 現在のデザインシステム（現状分析）

### 3-1. ポータルシェル（platform-core / platform.css）

```
パレット: Warm Monotone
  --cream-50:  #FDFBF8   コンテンツ背景
  --cream-100: #F7F3EE   サイドバー背景
  --cream-900: #2C1E12   ヘッダー背景
  --accent:    #8C6A50   アクセント（ブラウン）

フォント: Inter (Latin) + Noto Sans JP (CJK)
          font-weight: 300/400/500/600 のみ使用

レイアウト: grid（header 52px + sidebar 236px + main iframe）
            右端に DAP チャットパネル 296px（スライドイン）
```

### 3-2. 各モジュール（それぞれに独自 CSS、共有なし）

| モジュール | 主色 | 個性 |
|-----------|------|------|
| R&D評価（8003） | `#E8833A`（オレンジ） | `--bg:#FFF8F3` 暖色系、`.score-orb` 円形バッジ |
| 品目管理（8002） | `#E8833A`（同上） | 同じトークンを使用しているが CSS 非共有 |
| AI該非判定（8011） | `#E8833A`（同上） | 同上 |
| 取引先スクリーニング（8005） | `#E8833A`（同上） | 同上 |
| 特許検索（8004） | `#E8833A`（同上） | 同上 |

→ **モジュール間でデザイントークンは実質統一されているが、CSS ファイルが共有されておらず各モジュールにベタ書き（重複コード）**

---

## 4. 主要 UX パターン（現在）

### 4-1. R&D プロジェクト詳細（最も成熟したページ）

`/ui/cases/{id}/profiles/latest` — 最近刷新済み

```
[ワークフロー進捗バー] ①〜⑤ ステップ（刷新済み）
[バージョン基本情報カード]
[リスクスコアカード]
  ├─ スコアオーブ（overall）+ 4次元バーグラフ
  ├─ 推奨対応ラベル（日本語）
  └─ Explainability
       ├─ 開示戦略カード（バッジ+根拠リスト）（刷新済み）
       └─ セキュリティポスチャカード（カラーバッジ+因子チップ）（刷新済み）
[プロファイル本文（生テキスト <pre> 表示）]
[知財評価（IP Review）フォーム + エビデンスアップロード]
[AI該非判定セクション（2リスト照合テーブル）]
[取引先スクリーニングセクション]
[品目管理への登録セクション]
```

### 4-2. AI該非判定 取引詳細

```
[取引メタ情報]
[判定結果バナー（danger/warn/info/safe）]
[2リスト照合テーブル（両方一致/直接一致/特許示唆）]
[EAR Section 4 キャッチオールレポート（折りたたみ）]
[FAISS ベクター候補リスト]
[仕向地 EAR Country Chart リスクプロファイル]
```

### 4-3. 現在の共通コンポーネント（手書き、ライブラリなし）

- `.card` — `border-radius:14px`, `box-shadow`, `padding:24px`
- `.badge` — pill 型、色変数で種別分け
- `.chip` — ステータス表示（draft/approved/rejected 等）
- `.score-orb` — 円形スコア（直径 82px）
- `.score-track` + `.score-fill` — 水平プログレスバー
- `.kv` — key/value 2カラムグリッド表示
- `.diff-table` — バージョン比較差分テーブル
- `.btn`, `.btn-primary`, `.btn-ghost`, `.btn-sm` — ボタンバリアント

---

## 5. 現在の主な UX 課題（設計上の問題点）

### P1: モジュール内ナビゲーションの二重化
- ポータルのサイドバーでモジュールを選択 → iframe の中にさらにモジュール固有のヘッダーナビが表示される
- ユーザーは「2層のナビゲーション」を操作することになり混乱しやすい

### P2: iframe アーキテクチャの限界
- ページ間遷移がブラウザ履歴に反映されない
- ディープリンクが機能しない（`https://app.../` は常にポータルトップ）
- iframe ↔ ホスト間の通信は postMessage で一部対応しているが不完全

### P3: 情報密度の問題（過負荷）
- `profiles_latest.html` は 1 画面に 8 セクションを縦積み（スクロール 3,000px 以上）
- ユーザーが「今どこにいて、何をすべきか」がわかりにくい

### P4: CSS コードの重複
- 各モジュールが同じデザイントークン（`#E8833A` 等）を個別にハードコード
- デザイン変更時に 6〜8 ファイルを同時修正が必要

### P5: プロファイル本文が生テキスト
- R&D プロファイルの本文は `<pre>` タグで生テキスト表示
- 半構造化されたデータなのに視覚的な整理がない

### P6: 入力フォームの UX
- プロフィール新規入力フォーム（`profiles_new.html`）は長大な 1 画面フォーム
- チェックボックス多数・CSV 入力フィールドなど操作性に難あり

### P7: ステータス・リスクレベルの不統一
- 各モジュールで独自にステータス色・ラベルを定義
- `danger/warn/info` vs `critical/high/medium/low` が混在

---

## 6. 重要なデザイン制約

1. **SSR のみ** — フレームワーク（React/Vue 等）は使用不可。Jinja2 テンプレート + 素の JS
2. **外部 CSS/JS ライブラリ最小化** — 現在は Google Fonts のみ外部依存。Tailwind や Bootstrap は未使用
3. **日本語優先** — すべての UI テキストは日本語。英語は補助表記のみ
4. **法令対応の視認性** — リスクレベルの色分けは法的判断に直結するため、誤読を防ぐ明確な配色が必須
5. **業務ツールの性格** — デザインよりも情報密度・操作効率が優先される場面が多い
6. **iframe シェル維持** — ポータル全体の iframe 構造は短期では変更しない前提

---

## 7. 設計上の強み（維持すべき点）

- **Warm Monotone パレット**: ポータルシェルのウォームブラウン系配色は完成度が高く、堅牢な印象を与える
- **スコアオーブ**: リスクスコアの円形ビジュアライゼーションは直感的でわかりやすい
- **バッジシステム**: pill 型バッジ + 色変数の組み合わせは拡張しやすい
- **フォントスタック**: Inter + Noto Sans JP の組み合わせは日英混在テキストに適している
- **DAP チャットウィジェット**: 全モジュール埋込済みのコンテキスト認識 AI アシスタント

---

## 8. 設計改善の優先候補

### A（高優先度）— UX 構造
1. **ポータルナビゲーション整理**: iframe 内ナビとポータルナビの統合方針の検討
2. **R&D 詳細ページの情報アーキテクチャ**: 8 セクションのタブ化または段階的展開
3. **案件ダッシュボード**: 進行中案件の全体俯瞰ビュー（現状は機能が限定的）

### B（中優先度）— コンポーネント
4. **共通デザイントークン定義**: `platform.css` をすべてのモジュールから参照できる形に
5. **リスクレベル表現の統一**: `critical/high/medium/low` の色・ラベル・アイコンをシステム化
6. **プロファイル入力フォームの UX 改善**: ウィザード形式またはセクション折りたたみ

### C（低優先度）— 品質
7. **アクセシビリティ**: ARIA ラベル・フォーカス管理・キーボード操作対応
8. **プロファイル本文の表示改善**: 生テキスト → 構造化表示

---

## 9. 現在動作中の画面 URL（参照可能）

| 画面 | URL |
|------|-----|
| ポータル（シェル全体） | `https://app.tsp-aitrademanagement.com/` |
| 案件ダッシュボード | `https://app.tsp-aitrademanagement.com/ui/dashboard` |
| R&D プロジェクト一覧 | `https://app.tsp-aitrademanagement.com/proxy/rnd_assessment/ui/cases` |
| R&D プロジェクト詳細 | `https://app.tsp-aitrademanagement.com/proxy/rnd_assessment/ui/cases/{id}/profiles/latest` |
| AI該非判定 取引一覧 | `https://app.tsp-aitrademanagement.com/proxy/ai_validation/ui/transactions` |
| 品目管理 | `https://app.tsp-aitrademanagement.com/proxy/ai_classification/products` |
| EPA/FTA 特恵税率 | `https://app.tsp-aitrademanagement.com/ui/fta-check` |
| 調達・BOM 管理 | `https://app.tsp-aitrademanagement.com/ui/supply-chain` |
| プラットフォーム管理 | `https://app.tsp-aitrademanagement.com/ui/platform` |

---

## 10. 設計検討に際してのコンテキスト補足

### ターゲットユーザーの業務シーン

**輸出管理担当者（主要ユーザー）**
- 1 日に数件〜数十件の取引を審査
- 規制知識は十分だが、複数システムの切り替えが多くて非効率
- 判断の根拠を書類として残す義務がある（証跡管理）

**R&D チームリーダー（準主要ユーザー）**
- 月 1〜数件の新プロジェクト起案
- 法規制知識は浅い。AI の判断を信頼したい
- 知財戦略（特許 vs 秘匿）の意思決定を支援してほしい

**IT 管理者（サブユーザー）**
- モジュール起動状態の確認・サプライヤーポータル URL 発行

### 現在のデータ規模感
- 制裁リスト: 約 21,000 件（OFAC/BIS-EL/EU/UK 等 7 ソース）
- FAISS Layer A: 2,999 ベクトル（外為法/ECCN/USML/EU/Wassenaar）
- HS コード: 11,368 件（NACCS コードリスト）
- FTA 協定: 10 協定・代表 HS コード 8 種のサンプルデータ

---

*作成: 2026-05-07*
*開発状況: Priority B 完了・R&D UI 刷新完了（次: Priority C — グローバルコンプライアンス）*
